# CloudShelf credentials. The server refuses to start without these.
# Pick a strong password — this is the only thing standing between the
# internet and the S3 credentials in cloudshelf.db.
CLOUDSHELF_USERNAME=admin
CLOUDSHELF_PASSWORD=change-me

# Optional overrides
# PORT=3001
# CLOUDSHELF_DB=./data/cloudshelf.db
